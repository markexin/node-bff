{
  "name": "node-bff",
  "private": true,
  "scripts": {
    "cms": "lerna run --parallel cms",
    "cms:server": "lerna run --parallel cms-server",
    "debug:service": "lerna run --parallel service-debug"
  },
  "devDependencies": {
    "lerna": "^4.0.0"
  }
}
